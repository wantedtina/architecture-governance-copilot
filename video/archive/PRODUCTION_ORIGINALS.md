# Original production workspaces and backups

The original production collection is physically stored at:

`/Users/wantedtina/Repos/architecture-governance-copilot/video/archive/production-originals/`

It contains the original date-named iterations, tool-assisted workspaces, user recordings, final submission package, release receipts, protected V1 backup and reorganization backups. The entire directory is Git-ignored. Fresh checkouts contain this guide, not the local collection.

The former Deliverables directory and its compatibility symlink have been retired. Relevant scripts and consumed input manifests now refer to the project-local collection. Historical logs, command transcripts and protected backups retain original path strings as provenance, not current instructions. Text before path replacement is preserved in `path-migration-2026-09-15/before/` and changes are recorded in its receipt. Rendered media and original recordings are unchanged.

For normal browsing, start with [V1](../v1/README.md) or [V2](../v2/README.md). Future production must still use a separate revision workspace and recheck cached tools, application revision guards and output locations before executing historical scripts. Path migration does not make the reorganized pipelines fully portable or prove a fresh render. The protected V1 backup and earlier migration snapshots are preserved unchanged.

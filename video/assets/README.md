# Shared accepted artwork

`opening-title.png` and `closing-card.png` are common accepted assets used by both versions. They remain at their original paths for compatibility with external V1 source scripts. Exact version copies also appear under each version's slides directory. Earlier non-shared artwork is in `../archive/pre-v1/assets/`.
